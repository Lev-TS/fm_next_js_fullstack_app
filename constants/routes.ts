export enum PageRoutes {
  Home = "/home",
  Calendar = "/calendar",
  Profile = "/profile",
  Settings = "/settings",
  SignIn = "/signin",
  Register = "/register",
  Project = "/project",
}

export enum ApiRoutes {
  Register = "/api/register",
  SignIn = "/api/signin",
  CreateNewProject = "/api/project",
}
